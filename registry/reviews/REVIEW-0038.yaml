id: REVIEW-0038
status: OPEN
artifact:
  type: PULL_REQUEST
  id_or_path: "PR-5"
  commit_sha: null
reviewer:
  actor: null
  context_id: null
  independence_level: L2
roles:
  - ARCHITECTURE_REUSE
  - VERIFICATION_VALIDATION
  - SECURITY
  - PERFORMANCE_SRE
  - DOCUMENTATION_TRACEABILITY
  - RED_TEAM_SKEPTIC
external_import: null
scope:
  work_items: [WORK-0002]
  requirements: [REQ-0026]
  requirement_revisions: {}
  capabilities: []
  contracts:
    - "trusted default-branch stale-green review-state invalidation"
    - "protected MONDE / Merge Gate job/check is the merge-state authority"
    - "required-job lookup is bound to the current workflow run_attempt"
    - "distinct current-incarnation rerun targets expose exactly one valid protected gate job before rerun"
    - "success, neutral and skipped protected-check conclusions all trigger stale-green revalidation"
    - "current PR incarnation binding across branch resets and overlapping PR lifetimes"
    - "fail-closed GitHub REST/GraphQL metadata and bounded pagination"
    - "bootstrap regression and TEST-0009 traceability completeness"
  risks:
    - "A stale Jobs response from a prior workflow attempt may misclassify current protected-check state."
    - "A distinct target run may be rerun even though it cannot produce the protected MONDE / Merge Gate context."
    - "Actions Jobs pagination, malformed job metadata or duplicate required jobs may create a false merge-state classification."
    - "Shared-SHA and reused-branch histories may still expose wrong-context rerun paths."
findings: []
checks:
  assumptions_challenged:
    - "filter=latest alone proves every returned job belongs to the workflow run's current attempt."
    - "Validating only the effective shared-SHA run's protected job is enough when the rerun target is a distinct run."
    - "The previous thirteen negative reviews exhaust the relevant fail-open surface."
  omitted_categories_considered:
    - "workflow run_attempt versus job run_attempt authority"
    - "effective-run versus target-run protected-job parity"
    - "Actions Jobs API record validation, pagination and uniqueness"
    - "shared-SHA, reused-branch, force-push/reset and overlapping PR lifetimes"
    - "REST and GraphQL schema failure"
    - "bounded pagination and rate/latency behavior"
    - "least-privilege Actions permissions"
    - "requirement/test/work/review traceability"
  duplicate_or_reuse_search:
    - "REVIEW-0031 through REVIEW-0037 remain terminal negative evidence and are never reopened or rewritten."
    - "Bootstrap remains a temporary predecessor; PR #2's durable poller must later reuse/port the hardened contract rather than retain its older permissive implementation."
  failure_modes_considered:
    - "latest Jobs response contains a completed protected job from an older run attempt"
    - "effective shared-SHA run is valid but distinct target run has no valid MONDE / Merge Gate job"
    - "required job missing, duplicated, malformed, wrong run_id, wrong run_attempt or wrong head_sha"
    - "shared-SHA effective run differs from current-incarnation rerun target"
    - "historical PR branch reset or overlapping lifetime causes wrong-context rerun"
    - "malformed GraphQL or review-thread pagination suppresses revalidation"
  evidence_requested:
    - "Exact-head 100 percent line and branch suite including run-attempt and distinct-target regressions"
    - "Read-only live GitHub contract probe against WORK-0002 PR #2 through workflow-runs, Actions Jobs, PR-history and reviewThreads APIs"
    - "Synchronized 32/32 finding handover with REVIEW-0037 terminal state"
    - "Fresh-context exact-head Codex review after correction"
outcome: CHANGES_REQUIRED
completed_at: null
history:
  created_at: "2026-09-15"
  updated_at: "2026-09-15"
