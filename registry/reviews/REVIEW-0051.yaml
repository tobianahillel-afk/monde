id: REVIEW-0051
status: OPEN
artifact:
  type: PULL_REQUEST
  id_or_path: "PR-5"
  commit_sha: null
reviewer:
  actor: null
  context_id: null
  independence_level: L2
roles:
  - ARCHITECTURE_REUSE
  - VERIFICATION_VALIDATION
  - SECURITY
  - PERFORMANCE_SRE
  - DOCUMENTATION_TRACEABILITY
  - RED_TEAM_SKEPTIC
external_import: null
scope:
  work_items: [WORK-0002]
  requirements: [REQ-0026]
  requirement_revisions: {}
  capabilities: []
  contracts:
    - "trusted default-branch stale-green review-state invalidation"
    - "exact latest GitHub Actions-owned MONDE / Merge Gate check remains merge-state authority"
    - "proven REVIEW-0050 V5 poll and closed-origin pending observation behavior remains unchanged"
    - "operator recovery is inspection-only and never clears, writes or rewrites scheduler state"
    - "operator recovery never calls a workflow rerun API and has no actions:write permission"
    - "operator recovery has issues:read rather than issues:write permission"
    - "inspection requires the exact durable pending PR/head/run/baseline/check tuple, exact confirmation phrase and auditable reason"
    - "unchanged run attempt and effective-check identity are revalidated only to identify the intended pending record, never to authorize pending deletion"
    - "successful inspection preserves issue #7 pending authority and prints the exact canonical run for a separately authorized manual rerun"
    - "the normal serialized poll remains solely responsible for observing baseline+1 and terminally accounting for the durable pending mutation"
    - "if external/manual activity races or attribution becomes ambiguous, pending authority remains durable and processing fails closed"
    - "all prior exact-check, PR/base/merge-ref, pagination, request-budget, attempt, shared-head and fail-closed protections remain inherited"
  risks:
    - "A human can still make an incorrect independent judgment that the original POST never occurred before manually rerunning the exact run."
    - "A collaborator/UI/API rerun can race after inspection, but durable pending authority must remain intact so the race becomes observable rather than erased."
    - "The old REVIEW-0050 recovery module remains in history/test coverage and must never be the active workflow entrypoint for manual recovery."
findings: []
checks:
  assumptions_challenged:
    - "Removing recovery state mutation is sufficient to eliminate the REVIEW-0050 check-then-clear TOCTOU without weakening ordinary pending observation."
    - "Read-only inspection can provide enough exact operator context for a manual rerun while leaving causal accounting to the normal poll."
    - "Keeping the predecessor recovery module as historical code does not re-expose its clear path through the active workflow."
    - "A successful inspection followed by an external/manual rerun remains safe because issue #7 pending identity is never erased."
  omitted_categories_considered:
    - "workflow accidentally still invokes REVIEW-0050 recovery entrypoint"
    - "manual recovery retains issues:write or actions:write despite inspection-only design"
    - "inspection calls _write_state indirectly or directly"
    - "inspection calls rerun_workflow indirectly or directly"
    - "run attempt or prior check changes before inspection"
    - "external actor reruns after inspection completes"
    - "operator uses a different run than the exact run printed by inspection"
    - "origin PR is closed while pending head/run authority remains active"
    - "normal poll observes delayed baseline+1 after manual rerun"
  duplicate_or_reuse_search:
    - "REVIEW-0050 remains CLOSED/CHANGES_REQUIRED administrative negative evidence and is not rewritten."
    - "REVIEW-0049 remains COMPLETE/CHANGES_REQUIRED independent negative evidence."
    - "REVIEW-0051 reuses the REVIEW-0050 V5 poll/closed-origin implementation and replaces only the unsafe recovery authorization boundary."
    - "All 65 historical inline material PR #5 threads remain unresolved."
  failure_modes_considered:
    - "accepted inspection mutates issue #7 or clears pending state"
    - "accepted inspection emits an automatic rerun"
    - "workflow permissions allow manual recovery to mutate Actions or Issues"
    - "stale tuple, changed run attempt or changed effective-check identity is accepted"
    - "external rerun starts immediately after inspection; pending must remain to observe/attribute or fail closed"
    - "manual rerun creates baseline+1 but normal poll cannot resume exact pending observation"
    - "historical REVIEW-0050 recovery clear path is accidentally wired back into the active workflow"
  evidence_requested:
    - "Exact-head full bootstrap suite with 100 percent project-owned line and branch coverage including REVIEW-0051 adapter."
    - "Regression proving accepted recovery inspection cannot call _write_state or rerun_workflow."
    - "Workflow regression proving pending-recovery has actions:read and issues:read but neither actions:write nor issues:write."
    - "Read-only live GitHub contract probe against WORK-0002 PR #2 on the exact candidate head."
    - "Fresh-context independent L2 on one frozen exact HEAD after OPEN and IN_PROGRESS lifecycle checkpoints are themselves proved."
    - "Resolve no historical inline thread until the successor L2 is clean."
outcome: CHANGES_REQUIRED
completed_at: null
history:
  created_at: "2026-09-22"
  updated_at: "2026-09-22"
