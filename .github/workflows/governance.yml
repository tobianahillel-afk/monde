name: MONDE Gate

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

concurrency:
  group: monde-gate-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  governance-core:
    name: Governance core
    uses: ./.github/workflows/_governance-core.yml
    with:
      event_name: ${{ github.event_name }}
      base_sha: ${{ github.event.pull_request.base.sha || github.event.before || github.sha }}
      head_sha: ${{ github.event.pull_request.head.sha || github.sha }}

  dependency-review:
    name: Dependency review
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-24.04
    permissions:
      contents: read
    steps:
      - name: Review dependency changes
        uses: actions/dependency-review-action@a1d282b36b6f3519aa1f3fc636f609c47dddb294 # v5.0.0
        with:
          fail-on-severity: moderate

  codeql:
    name: CodeQL
    runs-on: ubuntu-24.04
    permissions:
      actions: read
      contents: read
      packages: read
      security-events: write
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Initialize CodeQL
        uses: github/codeql-action/init@b96794f015dfd88f77b49b1c93e0fa7110f94c63 # v4
        with:
          languages: python
      - name: Analyze
        uses: github/codeql-action/analyze@b96794f015dfd88f77b49b1c93e0fa7110f94c63 # v4

  final-gate:
    name: MONDE / Merge Gate
    if: always()
    needs:
      - governance-core
      - dependency-review
      - codeql
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    permissions:
      contents: read
      pull-requests: read
    steps:
      - name: Verify lane results
        shell: bash
        run: |
          set -euo pipefail
          echo "governance-core=${{ needs.governance-core.result }}"
          echo "dependency-review=${{ needs.dependency-review.result }}"
          echo "codeql=${{ needs.codeql.result }}"
          test '${{ needs.governance-core.result }}' = 'success'
          test '${{ needs.codeql.result }}' = 'success'
          case '${{ needs.dependency-review.result }}' in
            success|skipped) ;;
            *) exit 1 ;;
          esac

      - name: Checkout current PR merge ref
        if: github.event_name == 'pull_request'
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Verify live GitHub review state
        if: github.event_name == 'pull_request'
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: |
          python -m tools.governance.github_live_gate \
            --repo '${{ github.repository }}' \
            --pr '${{ github.event.pull_request.number }}' \
            --head '${{ github.event.pull_request.head.sha }}' \
            --json-out github-live-gate.json

      - name: Write final gate summary
        if: always()
        shell: bash
        run: |
          {
            echo '## MONDE Merge Gate'
            echo
            echo '- Deterministic governance: `${{ needs.governance-core.result }}`'
            echo '- Dependency review: `${{ needs.dependency-review.result }}`'
            echo '- CodeQL: `${{ needs.codeql.result }}`'
            echo '- Live PR review-thread/head state checked for pull requests.'
            echo
            echo '**Only this stable final gate should become the required MONDE status check.**'
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Upload live gate evidence
        if: always() && github.event_name == 'pull_request'
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: monde-live-gate-${{ github.event.pull_request.head.sha }}
          path: github-live-gate.json
          if-no-files-found: warn
          retention-days: 14
